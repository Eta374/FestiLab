<form method="post" action="{{ path('patners_delete', {'id': patner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ patner.id) }}">
    <button class="btn">Delete</button>
</form>
