{% extends 'base.html.twig' %}

{% block title %}Patners{% endblock %}

{% block body %}
    <h1>Patners</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ patner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ patner.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td><img class="img-miniature" src="{{ asset('uploads/artists_pictures/'~patner.picture) }}" alt="{{ patner.picture }}"></td>
            </tr>
            <tr>
                <th>WebsiteLink</th>
                <td>{{ patner.websiteLink }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('patners_index') }}">back to list</a>

    <a href="{{ path('patners_edit', {'id': patner.id}) }}">edit</a>

    {{ include('patners/_delete_form.html.twig') }}
{% endblock %}
