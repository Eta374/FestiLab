{% extends 'base.html.twig' %}

{% block title %}Edit Festivals{% endblock %}

{% block body %}
    <h1>Edit Festivals</h1>

    {{ include('festivals/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('festivals_index') }}">back to list</a>

    {{ include('festivals/_delete_form.html.twig') }}
{% endblock %}
