{% extends 'base.html.twig' %}

{% block title %}Kinds{% endblock %}

{% block body %}
    <h1>Kinds</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kind.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kind.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kinds_index') }}">back to list</a>

    <a href="{{ path('kinds_edit', {'id': kind.id}) }}">edit</a>

    {{ include('kinds/_delete_form.html.twig') }}
{% endblock %}
