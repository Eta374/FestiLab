{% if is_granted("ROLE_ADMIN") %}
<div class="col-lg-2">
<form method="post" action="{{ path('news_delete', {'id': news.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir effacer cette News ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news.id) }}">
    <button class="btn">Supprimer</button>
</form>
</div>
{% endif %}